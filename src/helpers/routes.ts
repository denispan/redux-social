interface IRoutes {
  auth: string;
  posts: string;
  post: string;
  profile: string;
}

export const routes: IRoutes = {
  auth: '/auth',
  posts: '/posts',
  post: '/post',
  profile: '/profile',
};
